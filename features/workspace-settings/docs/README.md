# Настройки workspace (Workspace Settings)

> 2026-05-30 · статус: полностью реализован и декомпозирован по **4-слойному стандарту** (`api`, `model`, `application`, `ui`). [Epic #196](https://github.com/huntechri/smetalabs/issues/196).

---

## Назначение

Модуль предназначен для администрирования рабочего пространства (workspace): управление списком участников команды, назначение и смена ролей, отправка приглашений по электронной почте, генерация пригласительных ссылок и настройка ограничений по доменам электронной почты.

**Маршрут:** `/team` (страница «Команда»)  
**Связанные таблицы:** `workspace_members`, `workspace_invitations`, `workspace_allowed_domains`

---

## Структура модуля

```
features/workspace-settings/
├── api/
│   ├── team-client.ts                      # HTTP-клиент (fetch → /api/team/*): участники, роли, домены, ссылки
│   ├── team-errors.ts                      # Типизированные ошибки API
│   ├── team-mappers.ts                     # Адаптеры сетевых ответов → внутренние типы модели
│   ├── team-query-keys.ts                  # Ключи кэширования React Query
│   └── __tests__/
│       └── team-client.test.ts             # Unit-тесты клиента
├── model/
│   ├── workspace-settings-model.ts         # Типы, статусы участников, бизнес-хелперы
│   └── workspace-settings-model.test.ts    # Unit-тесты модели
├── application/
│   ├── use-workspace-settings.ts           # Общий хук настроек воркспейса
│   ├── use-workspace-members.ts            # Хук получения и управления участниками
│   ├── use-workspace-overview.ts           # Хук сводных показателей воркспейса
│   ├── use-workspace-member-actions.ts     # Хук обработки действий над участником
│   ├── use-workspace-member-dialogs.ts     # Хук управления диалогами участников
│   ├── use-domains.ts                      # Хук управления разрешенными доменами
│   ├── use-invitations.ts                  # Хук управления email-приглашениями
│   ├── use-invite-link.ts                  # Хук управления публичными ссылками-приглашениями
│   └── use-invite-member.ts                # Хук отправки приглашений
├── ui/
│   ├── workspace-settings-view.tsx         # Входная точка настроек воркспейса (табы)
│   ├── team-management-view.tsx            # Представление управления командой
│   ├── workspace-overview-card.tsx         # Карточка сводного обзора воркспейса
│   ├── workspace-roles-summary-card.tsx    # Карточка со сводкой ролей команды
│   ├── workspace-members-table.tsx         # Таблица участников с колонками (десктоп)
│   ├── allowed-domains-card.tsx            # Настройка разрешенных почтовых доменов
│   ├── invite-link-card.tsx                # Управление публичной ссылкой-приглашением
│   ├── invite-member-card.tsx              # Форма отправки email-приглашения участнику
│   ├── pending-invitations-table.tsx       # Таблица активных/ожидающих приглашений
│   ├── remove-member-dialog.tsx            # Диалог исключения участника из команды
│   ├── suspend-member-dialog.tsx           # Диалог приостановки (блокировки) доступа
│   ├── reset-password-dialog.tsx           # Диалог сброса пароля участника
│   ├── role-change-dialog.tsx              # Диалог изменения роли участника
│   ├── workspace-actions-card.tsx          # Дополнительные настройки воркспейса
│   └── members/                            # Составные компоненты таблицы участников
│       ├── workspace-members-section.tsx   # Секция списка команды (загрузка/ошибка)
│       ├── workspace-members-table.tsx     # Таблица участников (десктопная версия)
│       ├── workspace-members-mobile-list.tsx # Список участников (мобильная версия)
│       ├── workspace-member-row.tsx        # Строка/карточка конкретного участника
│       ├── workspace-member-status-badge.tsx # Отображение статуса (Активен / Заблокирован)
│       ├── workspace-member-actions-menu.tsx # Выпадающее меню действий над участником
│       ├── workspace-members-skeleton.tsx  # Загрузочный скелетон участников
│       ├── workspace-members-error.tsx     # Отображение ошибок загрузки участников
│       └── workspace-member-dialogs.tsx    # Модальные окна действий над участниками
└── __mocks__/
    └── workspace-settings.ts               # Мок-данные для тестов
```

### Роутинг (App Router)
- `/team` — рендерит страницу `TeamPage` (вызывает `WorkspaceSettingsView` из `ui/`).

---

## Карта зависимостей слоёв

```
ui/  →  application/  →  api/
ui/  →  model/
application/  →  model/
api/  →  model/
```

Слой `ui/` никогда не обращается напрямую к `api/` или к Supabase. Все сетевые вызовы инкапсулированы в `api/team-client.ts` и оркестрируются хуками в `application/`.

---

## Данные

### Таблицы базы данных

- **`workspace_members`** — список всех пользователей, привязанных к текущему владельцу воркспейса (`owner_id`), со статусом (`active`, `suspended`, `invited`) и аудитом времени.
- **`workspace_invitations`** — активные email-приглашения в команду со сроком жизни и статусом.
- **`workspace_allowed_domains`** — доменные ограничения для регистрации в данном воркспейсе.

---

## API (Next.js Route Handlers)

Все эндпоинты настроек воркспейса размещены под роутом `/api/team/*` и используют серверную проверку прав (`team.manage`):

- `GET /api/team/members` — Получить список участников воркспейса.
- `POST /api/team/members` — Назначить роль участнику воркспейса.
- `DELETE /api/team/members` — Снять роль с участника (запрещено для locked-ролей).
- `GET /api/team/domains` — Разрешенные почтовые домены для воркспейса.
- `POST /api/team/domains` — Зарегистрировать новый разрешенный домен.
- `DELETE /api/team/domains` — Удалить разрешенный домен.
- `GET /api/team/invitations` — Список отправленных и не принятых email-приглашений.
- `POST /api/team/invitations` — Отправить новое приглашение на email.
- `DELETE /api/team/invitations` — Отозвать отправленное приглашение.
- `GET /api/team/invite-link` — Получить параметры действующей публичной пригласительной ссылки.
- `POST /api/team/invite-link` — Сгенерировать или сбросить публичную пригласительную ссылку.
- `GET /api/team/overview` — Сводные показатели по воркспейсу (количество человек, роли, активность).

---

## Текущее состояние

| Задача | Статус | Комментарий |
|---|---|---|
| **4-слойная декомпозиция** | ✅ Готова | `api`, `model`, `application`, `ui` — разделены согласно стандарту. |
| **БД-схема и RLS** | ✅ Готова | Таблицы участников, приглашений и доменов защищены RLS. |
| **API Route Handlers** | ✅ Готовы | Все 11+ эндпоинтов управления командой реализованы. |
| **Клиентские хуки** | ✅ Готовы | Хуки в `application/` интегрированы с React Query. |
| **Участники (Таблица)** | ✅ Готова | Десктопная таблица и мобильный список полностью интерактивны. |
| **Управление ролями** | ✅ Готово | Диалог смены роли участника работает. |
| **Блокировка доступа** | ✅ Готова | Приостановка (`suspend`) и активация участников работают. |
| **Email-приглашения** | ✅ Готовы | Пошаговая форма отправки и отзыв приглашений реализованы. |
| **Публичные ссылки** | ✅ Готовы | Генерация, копирование и сброс инвайт-ссылок работают. |
| **Разрешенные домены** | ✅ Готовы | Добавление и удаление доменных ограничений функционируют. |
| **Unit-тесты модели** | ✅ Готовы | `model/workspace-settings-model.test.ts` покрывает бизнес-хелперы. |
| **Unit-тесты API-клиента** | ✅ Готовы | `api/__tests__/team-client.test.ts` покрывает сетевой слой. |
